// Transformer la fonction en fonction fléchée
const winBattle = () => true;

// Utiliser l’opérateur ternaire pour attribuer des points d’expérience
const experiencePoints = winBattle() ? 10 : 1;

// Afficher les points d’expérience
console.log(experiencePoints); // 10
